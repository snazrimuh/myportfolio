# ─── Database (PostgreSQL / Neon) ────────────────────────────────────────────
# Pooled URL (pgbouncer) — untuk runtime queries
# Dapatkan dari: Neon dashboard → Connection Details → Pooled connection
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/portfolio?sslmode=require&pgbouncer=true&connect_timeout=15"

# Direct URL — untuk prisma migrate deploy
# Dapatkan dari: Neon dashboard → Connection Details → Direct connection
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/portfolio?sslmode=require"

# ─── JWT ──────────────────────────────────────────────────────────────────────
JWT_SECRET="your-super-secret-jwt-key-change-this"
JWT_EXPIRES_IN="1d"

# ─── Admin (used during seeding) ──────────────────────────────────────────────
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-this-password"

# ─── App ──────────────────────────────────────────────────────────────────────
PORT=3001
FRONTEND_URL="http://localhost:3000"
